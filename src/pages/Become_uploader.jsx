import React from 'react';

const Become_uploader = ({user}) => {
    return (
        <div>
            Please Contact With Admin or Supervisor
        </div>
    );
};

export default Become_uploader;